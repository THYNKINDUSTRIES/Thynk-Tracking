# Google Sheets Configuration
# Get your Sheet ID from the URL: https://docs.google.com/spreadsheets/d/[SHEET_ID]/edit
GOOGLE_SHEET_ID=your_google_sheet_id_here

# Google Service Account Credentials (JSON format)
# For production deployment, paste the entire service account JSON as a single line
# For local development, you can use service_account.json file instead
GOOGLE_CREDENTIALS_JSON={"type":"service_account","project_id":"..."}

# CORS Configuration
# Comma-separated list of allowed origins for CORS
# Use '*' for development, specific domains for production
# Example: https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=*

# Flask Configuration
FLASK_ENV=production
FLASK_DEBUG=False
